# Infralink v0.6.8

This patch release makes registry SSH transport trust an explicit, typed
controller-bootstrap input. Bootstrap now fails before remote mutation when
the declaration omits or contains malformed `registry_known_hosts` material.

The value travels through the existing typed bootstrap executor envelope. This
lets a controller runtime obtain private registry trust from the private
registry declaration rather than from a baked public image.
