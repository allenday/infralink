# Infralink v0.6.5

This patch release makes Gatus-backed Doctor evidence resolve through the
generated Gatus fragment in the selected Registry checkout. It no longer
assumes a renderer identity equals Gatus's display name.

The mapping is fail-closed: missing, malformed, stale, or ambiguous rendered
identity artifacts produce a typed input error instead of misleading health.
