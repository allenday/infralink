# Infralink v0.6.26

This patch restores the canonical public doctor target syntax after the typed
Agent Surface projection: `infralink doctor host <ref>`. The generated CLI
help again identifies the target kind and reference as positional arguments,
while MCP retains its typed request fields.
