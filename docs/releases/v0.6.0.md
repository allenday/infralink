# Infralink v0.6.0

This minor release exposes the full non-recursive Infralink command tree as
generated native MCP tools. Each tool uses the existing CLI command metadata,
dispatch, validation, and HATEOAS result envelope; `mcp serve` is excluded to
prevent recursive invocation.
