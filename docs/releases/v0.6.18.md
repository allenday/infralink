# Infralink v0.6.18

This patch lets `host bootstrap` plan and apply a valid
`self-deploy.preservation-state.v1` host contract without requiring unrelated
Compose controller-image fields. Preservation-state bootstrap uses the
invoking controller image seed; ordinary desired-state contracts continue to
require their declared controller image.
