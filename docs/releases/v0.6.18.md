# Infralink v0.6.18

This patch adds preservation-state support to `host bootstrap` without
requiring unrelated Compose controller-image fields. A normal host deployment
also requires Infralink Ops v0.2.70 or later, which forwards the invoking
controller image seed to this command. Ordinary desired-state contracts
continue to require their declared controller image.
