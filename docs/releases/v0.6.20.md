# Infralink v0.6.20

This release adds typed, profile-declared connection contracts. Service
instances bind those contracts to component edges, allowing generic renderers
and observation consumers to use validated endpoint targets without inventing
an alternate topology representation.
