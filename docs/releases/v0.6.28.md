# Infralink v0.6.28

This patch lets the canonical `infralink` interface inspect its own host's
bounded reconcile evidence without a redundant SSH round trip. Remote host
inspection remains on the declared SSH transport.
