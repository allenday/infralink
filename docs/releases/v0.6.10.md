# Infralink v0.6.10

This patch release adds the portable, typed firewall declaration contract used
by registry-backed host definitions. Rendering and host realization remain in
the separate Infralink Ops runtime.

The release pipeline now verifies that built wheel metadata matches the
requested release version before it signs, tags, or publishes assets. This
prevents release tags from disagreeing with the installed package version.
