# Infralink v0.6.29

This patch makes ordered BWS project resolution, bounded secret auditing, and
declared topology diagram artifact generation available through the canonical
typed `infralink` Click and MCP surface.
