# Infralink v0.6.19

This patch correctly identifies a firewall ingress listener by protocol, bind
address, and port. Multiple services may therefore declare the same port on
distinct addresses, while duplicate ownership of the same address remains
invalid.
