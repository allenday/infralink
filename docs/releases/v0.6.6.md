# Infralink v0.6.6

This patch release makes host bootstrap fail before mutation when an operator
tries to author from the deployed controller cache. It also requires the
declared SSH host-key fingerprint directly from the host manifest, instead of
looking for a legacy generic operations contract.

The typed operator bootstrap operation is now the common boundary for its
public projections.
