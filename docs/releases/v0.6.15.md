# Infralink v0.6.15

This patch release publishes the existing public package through the configured
PyPI trusted-publishing workflow. It contains no runtime, registry, controller,
or topology behavior changes.
