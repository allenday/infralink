# Infralink v0.6.14

This patch release exposes the V2 artifact-binding projection from stable
source bytes. Generic consumers can validate the exact bytes they will
materialize while retaining Infralink's bounded YAML parsing, canonical-value
rules, provenance, and typed diagnostics.

It adds no deployment authority, provider behavior, or service-specific
Gatus, Grafana, or RelayOS logic.
