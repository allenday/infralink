# Infralink v0.6.1

This patch release fixes native MCP error handling: failed CLI commands now
retain the standard JSON HATEOAS envelope instead of inheriting the YAML default
from the enclosing `mcp serve` process.
