# Infralink v0.6.27

This patch projects the canonical `controller_managed` observation attribute
for controller-managed hosts. It replaces the retired V2 reconcile flag in
public observation plans without introducing a second deployment selector.
