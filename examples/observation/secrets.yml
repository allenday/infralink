schema_version: infralink.observation/v1
provider_aliases:
  - id: ci-database-password
    provider: opaque-secret-backend
    project: example-project
    object_id: database-password
secret_bindings:
  - {id: database-password, slot_id: password, alias: ci-database-password, delivery: environment, renderer_binding_id: postgres-env}
renderer_binding_identities:
  - {id: postgres-env, renderer: opaque-env-renderer, binding_ref: synthetic/postgres, delivery_forms: [environment]}
